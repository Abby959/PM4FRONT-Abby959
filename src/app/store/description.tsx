import Card from '@/components/card'
import React from 'react'

function page() {
    return (
      <Card id={0} name={''} description={''} price={0} stock={0} image={''} categoryId={0} />
    )
  }
  
  export default page;